import { createApp } from 'vue'; // importer la fonction createApp
import App from './App.vue'; // importer le composant App

createApp(App).mount('#app'); // monter l'application sur l'élément #app du DOM pour afficher le composant App et ses enfants
// cela permet de créer une instance de l'application et de monter le composant App sur l'élément #app du DOM
